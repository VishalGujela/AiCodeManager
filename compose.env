# Defaults for docker-compose port mapping (v1-compatible; no ${VAR:-default} in YAML).
# Usage: docker compose --env-file compose.env -f docker-compose.yml up -d
# scripts/dcompose sets these in the shell if unset.
BIND_IP=0.0.0.0
HOST_PORT=8000
